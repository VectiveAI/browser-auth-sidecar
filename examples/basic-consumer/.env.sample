# Basic consumer example — environment variables.
# These should match the values used by the sidecar's .env.

# BAS_CONTAINER_PREFIX=browser-auth
# BAS_CDP_PORT=9223
# BAS_SHARED_DIR=../../data/shared
# BAS_NETWORK=browser-auth-net
